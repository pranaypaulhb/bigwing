# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    fields:
      - { name: Title }
backend:
  name: github
  repo: sfinite/bigwing
  preview_context: my-provider/deployment
media_folder: "images/uploads"
